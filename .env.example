# Django environment configuration (example)

# Debug mode (True for development, False for production)
DEBUG=True

# Secret key (replace with a strong random key in production)
SECRET_KEY=dev-secret-key-only

# Allowed hosts (adjust for your environment)
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,192.168.242.129
